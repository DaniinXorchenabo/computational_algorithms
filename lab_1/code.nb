
pFunc[arr_, x_] := (result = 0;
                        For[i = 1, Count[arr,_] >= i, i++, result = (result * x) + Part[arr, i]; ]; 
                        result);

sinX[oldX_, eps_] := (
        x = Mod[Abs[oldX], 2 * Pi] * (oldX / Abs[oldX]);
        x = If[Abs[x] >= Pi, -Mod[Abs[x], Pi], Mod[Abs[x], Pi]] * (oldX / Abs[oldX]);
        x = If[Abs[x] > Pi / 2, Pi - Abs[x], x]* (oldX / Abs[oldX]);
        x = x;
        u = x;
        result = 0;
        For[k = 0,
            (Abs[u] > eps) || (k < 2), 
            u = -1 * x ^2 * u / (2 * k * (2 * k + 1)),
            k++;
            result += u;
            ];
        {N[result, 20], N[u, 20], k, N[x, 20]};
        N[result, 20]
);
    
cosX[oldX_, eps_] := (sinX[oldX + Pi/2, eps]);

expX[x_, eps_] := (
    result = 0;
    u=1;
    For[k = 0,
        (Abs[u] > eps),
        u = x * u / k,
        k++;
        result += u;
    ];
    N[result, 20]
);

{pFunc[arr1, x1], pFunc[arr2, x2], sinX[cosArg, eps], cosX[sinArg, eps], expX[expArg, eps]};
N[pFunc[arr1, x1] / pFunc[arr2, x2] * sinX[cosArg, eps] + cosX[sinArg, eps] / expArg * expX[expArg, eps], 20]

